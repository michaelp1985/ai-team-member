{
  "name": "github-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc && esbuild index.ts --bundle --platform=node --target=node22 --outfile=dist/nodejs/node_modules/github-client/index.js"
  },
  "devDependencies": {
    "@types/node": "^24",
    "esbuild": "^0.25",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3",
    "@octokit/auth-app": "^7",
    "@octokit/rest": "^21"
  }
}
